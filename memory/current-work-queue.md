# Текущая Очередь Работы

Этот файл хранит текущую декомпозированную очередь задач для активной реализации.
Держи его коротким и обновляй, когда практические следующие шаги существенно меняются.

Полная дорожная карта — в `memory/topic-product-strategy.md`. Канонические требования — в `technical-specification-mvp.md`.

## Текущее Состояние (на 2026-06-28)

- Фаза 0 закрыта: спрос (450K/мес, 88 фраз) ✓, экономика РСЯ (модель 4.5K→54K ₽/мес) ✓, данные (20 актов + полнотекстовой доступ) ✓, инфраструктура (Wordstat, Yandex Search, гибридный поиск) ✓.
- Фаза 1.1 закрыта: ядро доказано — DeepSeek v4-pro извлекает структуру акта качества уровня верифицированного эталона (A/B-тест с Gemini: не уступает).
- Фаза 1.1+ закрыта: анти-галлюцинация — промпт с запретом + верификатор `scripts/verify_citations.py`; 4 выдуманных ссылки → 0.
- Интеграция с Antigravity проверена: первый пилотный акт `yQ8qgPoesvWJ` обработан без галлюцинаций, результат верифицирован ✓.
- Правило стиля LLM закрыто: запрет приветствий, мета-фраз и описаний процесса добавлен в `scripts/prompt_practice_analysis.txt`, `scripts/prompt_user_story.txt`, `scripts/prompt_extract.txt` и уже был в `data/inbox/_TASK.md`.
- Лимиты Yandex Search API проверены и закреплены в `assistant-memory.md`; сбор 50–100 актов не упирается в квоты web/search.
- Фаза 1.3 начата: `find_cases.py` усилен фильтром `sudact.ru/regular/doc/`, retry, задержкой и дедупликацией; `extract_act_text.py` получил batch-режим из CSV.
- Собрана первая партия: `output/cases_ozon_regular.csv` — 87 уникальных URL актов; в `data/raw_acts/` скачано 50 raw-актов.
- Фаза 1.3 по md-артефактам закрыта: все 49 pending-актов из `data/inbox/_queue.json` обработаны Gemini-3.5-flash, создано 50 `user_story_*.md` и 50 `practice_*.md`, вся партия верифицирована `verify_all.py` (0 галлюцинаций) ✓.
- Контракт JSON v2 закрыт по первой партии: полный комплект на акт = `user_story_<docid>.md` + `practice_<docid>.md` + `structure_<docid>.json`. Все 50 JSON имеют `processing.status: "complete"`: 8 успел Antigravity, 42 добиты DeepSeek API на `deepseek-v4-pro` (исторический факт, не разрешение на будущий API без отдельного подтверждения). `validate_structures.py` и `verify_all.py` проходят без ошибок ✓.
- Словари enum согласованы и закреплены в `data/reference/zpp_enum_dictionary.json`: `dispute_type_code` строится от конструкции ЗоЗПП, `claim_type_codes` — от способов защиты/требований. Ситуационные детали остаются в `platform_or_company`, `object_type`, `object_name`, `situation_tags`.
- Enum-таксономия доведена до всех 50 JSON: 25 reviewed-значений перенесены из `data/review/phase1-2-enum-suggestions.reviewed.csv`, ещё 25 дозаполнены по словарю `data/reference/zpp_enum_dictionary.json`. Во всех 50 `structure_*.json` есть `taxonomy.dispute_type_code` и `taxonomy.claim_type_codes`; для `qNPGP4ky266p` сохранена hold-логика. Проверки `validate_structures.py` и `verify_all.py` проходят на 50/50; коммиты: `e8e278e`, `8638488`, `beb750d`.
- Фаза 1.4 первичной кластеризации выполнена: создана карта `data/review/phase1-4-case-clusters.csv` на 50 дел и отчёт `data/review/phase1-4-cluster-summary.md`. Получено 8 кластеров: 1 pillar, 4 landing, 1 long-tail, 2 hold-группы; embeddings пока не нужны.
- Ручная валидация кластеров выполнена в `data/review/phase1-4-cluster-validation.md`. Для SSG-прототипа выбраны 5 типов страниц: `goods_defect_art18`, `distance_sale_return_art26_1`, `info_violation_art10_12`, `service_refusal_art32`, `prepaid_goods_delay_art23_1`; `work_service_defect_art29` — резерв, `contract_validity_non_zpp`/`non_consumer_hold` — hold.
- SSG-прототип спроектирован в `data/review/phase1-5-ssg-prototype-design.md`: зафиксированы 5 маршрутов, шаблон страницы-ситуации, контракт карточки дела, фильтры, disclosure-блок, минимальные JSON-поля и ограничения по `timeline`/`amounts`.
- Первый SSG-прототип сгенерирован: `scripts/generate_ssg_prototype.py` пишет `site_prototype/index.html`, CSS и 5 HTML-страниц-ситуаций по выбранным кластерам. Проверки `validate_structures.py`, `verify_all.py` и быстрый HTML-check проходят.
- В SSG-прототип добавлен слой страниц конкретных дел; после расширения корпуса актуальный путь — `site_prototype/zpp/dela/<docid>/index.html`, сейчас генерируется 257 активных страниц дел с пользовательской историей, требованиями/результатом, выводами, применёнными нормами и ссылкой на судебный акт.
- Выполнен UX-проход по SSG-прототипу: уточнены заголовки, CTA, фильтры, sticky-sidebar, дисклеймеры, lead страниц дел, блоки правового разбора и пользовательские формулировки. Пользовательский интерфейс предварительно согласован; слово «Прототип» убрано из публичных страниц. Прототип пересобран, проверки проходят.
- Блок `Подробный правовой разбор` на страницах дел нормализован на уровне рендера для legacy/new `practice_*.md`; правила UI/рендера см. `memory/topic-ssg-prototype-ui.md`.
- Дедупликация парсинга решена для MVP: канонический источник — raw/structured-артефакты, быстрый индекс — `data/registry/case_registry.csv`, генератор — `scripts/build_case_registry.py`, поиск `scripts/find_cases.py` по умолчанию исключает уже известные `docid`/URL. Подробности: `memory/topic-parsing-deduplication.md`.
- Критерии и обоснования парсинга закреплены в `memory/topic-parsing-strategy.md`: приоритет ситуаций над регионами, источник Sudact на ближайшую партию, лимиты, hold-критерии, правила решений агента и путь масштабирования.
- План наполнения сайта закреплён в `memory/topic-content-fill-plan.md`: целевой корпус первой публичной версии 150–250 валидных дел ЗоПП, 8–12 страниц-ситуаций, пороги по ситуациям/регионам/судам и инстанционный слой.
- Универсальный повторяемый контракт парсинга для ИИ-агента создан в `data/review/_TASK_PARSE_NEXT_BATCH_AGY.md`: агент сам находит незавершённую партию или создаёт новый `data/parse_batches/YYYY-MM-DD-zpp-next[-N]/`, перед поиском определяет приоритетные кластеры по текущему покрытию, обновляет реестр, формирует кандидатов, скачивает raw, структурирует акты и оставляет `batch_state.md`. Перечень кластеров открыт; новые повторяющиеся ЗоЗПП-ситуации фиксируются как кандидаты, но словарь не меняется без подтверждения пользователя.
- Публичное имя проекта выбрано под широкую рамку сервиса: **Судпрактика Онлайн** / `sudpraktika-online`; ЗоПП остаётся первым разделом, а не названием всего сервиса. Домен `sudpraktika-online.ru` требует решения о продлении до 2026-07-01.
- Позиционирование уточнено: полезность сервиса строится не только на историях дел, но и на систематизации практики по ситуациям, требованиям, исходам, регионам и конкретным судам. Для страниц-ситуаций в ТЗ добавлен будущий блок алгоритмических рекомендаций по текущей выборке.
- Механизм обновления страниц-ситуаций: новые продуктовые блоки добавляются в единый SSG-шаблон `scripts/generate_ssg_prototype.py`, затем прототип пересобирается целиком; ручные HTML-правки страниц не используются, чтобы не ломать стиль и структуру.
- Инстанционный слой закреплён как часть будущей полезности: первичный фокус — СОЮ, затем по тем же категориям/ситуациям добираются апелляция, кассация, ВС РФ и при необходимости КС РФ; АС/ВАС — второй глобальный этап, не смешивать с текущим ЗоПП-разделом.
- Для будущих изменений шаблонов, CSS и данных нужен автоматический контроль: решение зафиксировано в `memory/topic-visual-regression-testing.md` — `test:data` + Playwright smoke/visual snapshots, без внешних сервисов на старте.
- Пилотный сегмент: маркетплейсы (Озон/ВБ) + процессуальные темы (по данным рыночного замера).
- Вторая партия из 55 судебных актов (batch-2026-06-27-zpp-next) успешно структурирована и валидирована ✓.
- Batch-2 (`2026-06-27-zpp-next-2`) приведён в консистентное состояние: 50/50 кандидатов имеют полную триаду, но 32 docid пересекаются с предыдущим batch; фактически новых для корпуса — 18. Общая база: 217 raw / 217 structured JSON. Валидация пройдена ✓.
- **Batch-2 исправлен после проверки:** нестандартные enum-коды нормализованы, спорные candidate-ситуации и результаты, полученные через внешний API без разрешения текущей задачи, оставлены в `hold`; неподтверждённые ссылки `123-ФЗ` и `4015-1` удалены. `validate_structures.py`: 217/217 OK; `verify_all.py`: 217/217 OK.
- Batch-3 (`2026-06-27-zpp-next-3`) проверен и исправлен после остановки внешних агентов по лимитам: 80 новых raw/structured-триад приведены в консистентное состояние; очередь `data/inbox/_queue.json` синхронизирована, все 297 известных docid имеют статус `done`.
- Общая база после batch-3: 297 raw / 297 structured JSON. `validate_structures.py`: 297/297 OK; `verify_all.py`: 297/297 OK. Валидатор усилен защитой от индексации `contract_validity_non_zpp`/`non_consumer_hold`, `claim_type_codes: hold` и несогласованных `index_policy`/`main_site_fit`.
- Кандидатные ЗоЗПП-ситуации batch-3 приняты и добавлены в `data/reference/zpp_enum_dictionary.json`: `proper_quality_goods_exchange_art25` (2 дела), `unfair_terms_imposed_services_art16` (11), `harm_from_defect_art14` (9), `consumer_material_damage_art35` (23). 45 дел переведены из `hold` в индекс.
- Статический прототип пересобран: общая главная `/`, раздел `/zpp/`, 11 страниц-ситуаций и 257 активных страниц-дел ✓.
- Локальный Docker Compose сервер настроен: `site` отдаёт `site_prototype/`, `caddy` служит входной точкой и будущим HTTPS-слоем; локальный адрес — `http://localhost:8080/`. Проверены главная и страница ситуации через HTTP 200 ✓.
- Базовый технический SEO для SSG-прототипа реализован: canonical URL на всех HTML-страницах, `sitemap.xml` на 270 URL и `robots.txt` генерируются из `SITE_PUBLIC_URL`.
- Pre-production URL-решение выполнено: ЗоПП-страницы перенесены под `/zpp/` (`/zpp/praktika/...`, `/zpp/dela/...`), старые root-level `/praktika/...` и `/dela/...` больше не генерируются.
- Минимальный контур регрессионных тестов внедрён: `npm run test:data` проверяет structured/markdown consistency; `@playwright/test` даёт 9 smoke-тестов и 14 visual snapshot-тестов; `npm run site:test` проходит на локальном Docker-сервере ✓.
- Публичный ребрендинг выполнен: README, npm metadata, канонические файлы памяти/ТЗ и SSG-шапка используют “Судпрактика Онлайн”; локальная папка `zpp-consult` не переименовывалась, чтобы не ломать окружение перед GitHub/CI.
- Нормализация legacy `user_story_*.md` под стандартные подзаголовки завершена: 145/145 предупреждений закрыты, `validate_structures.py --check-user-story-format` показывает 297/297 OK; строгая проверка включена в `npm run test:data`.
- Удалённый GitHub-репозиторий создан и привязан как `origin` (`git@github.com:phoenix0597/sudpraktika-online.git`); последний push пользователем выполнен, новые локальные коммиты после настройки CI нужно отправить и проверить в Actions.
- GitHub Actions CI настроен: `.github/workflows/ci.yml` запускает `npm run test:ci` на `push`/`pull_request` в `main`, сохраняет Playwright artifacts; локально `npm run test:ci` проходит ✓.

## Следующий Шаг

1. Push текущих локальных коммитов в GitHub и проверить первый GitHub Actions run.
2. Подготовить PROD-deploy контур: production `.env`, GitHub Secrets, deploy workflow по SSH, порядок ручного запуска/rollback.
3. Следующую партию парсинга запускать уже с учётом 11 принятых ситуаций и действующего контракта.

## Активные обсуждения

Открытые многосторонние обсуждения по контракту `memory/topic-multi-agent-discussion.md`. Любой агент при восстановлении контекста может подхватить обсуждение по ссылке и текущей стадии.

| Дискуссия | Стадия | resolves_to |
|---|---|---|
| `memory/discussions/statistics-analytics.md` — продуктовый инкремент «статистика/аналитика» | round-1 | `memory/topic-statistics-analytics.md` |

## Извлечение через агентный mailbox / LLM API

Два пути извлечения структуры акта:
1. **ИИ-агент с собственной моделью** (Antigravity, Mimo, ZCode, Codex или другой) — основной масштабируемый путь через файловый mailbox.
2. **Внешний LLM API** (например, DeepSeek API / `scripts/complete_structures_deepseek.py`) — только по явному разрешению пользователя в конкретной задаче; не является автоматическим fallback для агента.

Интеграция через «почтовый ящик»:
- Python наполняет очередь: `data/inbox/_queue.json` (`scripts/queue_for_agent.py`).
- ИИ-агент читает инструкцию: `data/inbox/_TASK.md` (роль, 3 задачи, анти-галлюцинация, JSON v2, формат).
- Агент пишет результаты: `data/structured/user_story_<docid>.md` + `practice_<docid>.md` + `structure_<docid>.json`; JSON считается готовым только при `processing.status: "complete"`.
- Python фиксирует source-метаданные при парсинге/бекфилле: `source_url`, домен, путь к акту, SHA-256 текста.
- Вывод проходит через `verify_citations.py`/`verify_all.py` и `validate_structures.py` (анти-галлюцинация и полнота структуры не зависят от модели).
- Режимы: ручной запуск агента (MVP) → автоматический CLI. Агент не использует внешний LLM API и не запускает API-скрипты без явного разрешения пользователя; если лимитов/модели не хватает, фиксирует blocker и останавливается.

## Задачи в очереди (точечные)

1. **Синхронизировать GitHub** — ВЫПОЛНЕНО для предыдущего состояния; после текущего CI-коммита нужен push.
2. **Настроить CI через GitHub Actions** — ВЫПОЛНЕНО: workflow добавлен, локальный `npm run test:ci` проходит; после push проверить первый Actions-run.
3. **До production-публикации перенести публичные URL ЗоПП под `/zpp/`** — ВЫПОЛНЕНО: генератор, ссылки, canonical, sitemap и smoke/visual-тесты обновлены; старые `/praktika/...` и `/dela/...` больше не генерируются.
4. **Передать выбранному ИИ-агенту задачу на следующую партию парсинга/структурирования** — достаточно команды: «Восстанови контекст. Сделай парсинг очередной партии судебных актов». Агент должен найти `data/review/_TASK_PARSE_NEXT_BATCH_AGY.md` и следовать повторяемому контракту. *(Повторяющаяся задача)*
5. **Донастроить `timeline` и `amounts` точечно** только если прототип покажет реальные дыры в шаблоне.
6. **Нормализовать legacy `user_story_*.md` под стандартные подзаголовки** — ВЫПОЛНЕНО: 297/297 историй проходят `--strict-user-story-format`; строгая проверка включена в `npm run test:data`.

## Затем (Фаза 1 — продолжение)

- Фаза 1.4: кластеризация актов по ситуациям (эмбеддинги + ручная валидация).
- Фаза 1.5: генерация страниц (SSG Astro/Markdown) по структуре из ТЗ + служебные SEO-страницы (region/court).
- Фаза 1.6: публикация + базовый SEO (schema WebSite/Organization/Article, sitemap).

## Отложенное

- Скриншоты через vision-модель (DeepSeek не vision; кандидат — YandexGPT Vision на ключе Yandex).
- Шаг 4 — лиды юристам + передача ПДн (отложено пользователем; фокус — РСЯ).
- DOCX-генератор — пост-MVP.
- Дополнительные каналы трафика — Фаза 9.
- После обкатки контракта многосторонних обсуждений на других thread'ах: промоутить процедуру в глобальную память (`C:\Users\Stas\.codex\memories\`) и синхронизировать с репозиторием `~/.codex/codex-memory-kit` с последующим push.
