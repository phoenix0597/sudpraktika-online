# 2026-06-29

## Проверка переобработки `practice_*.md` по применению норм

- Проверен отчёт внешнего агента по задаче `data/review/_TASK_REPROCESS_PRACTICE_NORM_APPLICATION_DEBT.md`.
- Основной долг по 13 опубликованным страницам закрыт: `data/review/practice-norm-application-debt.txt` пустой, слабая заглушка `Суд сослался на эту норму при разрешении спора` не используется.
- В 8 `practice_*.md` остаётся 162 нейтральных подпункта `Суд привёл эту норму в составе правового обоснования, но отдельно её применение не раскрыл.`; это допустимый безопасный вариант для норм, которые акт перечисляет без отдельного применения, а не долг по слабой заглушке.
- При проверке выявлены сопутствующие дефекты: часть `practice_*.md` получила смешанные заголовки вида `### 1. Нормы...`; в корпусе также оставались латинские `GК/GПК` вместо кириллических `ГК/ГПК`.
- Исправлено: заголовки правового разбора нормализованы, латинские буквы в русских правовых аббревиатурах заменены по всему корпусу `practice_*.md`, SSG пересобран.
- В `scripts/validate_structures.py` добавлена строгая проверка этих классов дефектов; `npm run test:data` теперь ловит их автоматически.
- Проверки после исправления: `npm run test:data` — OK, `npm run test:citations` — 297/297 без галлюцинаций, `npm run test:smoke` — 10/10.

Причина дефекта: задача агенту и тесты контролировали главный симптом — отсутствие `Применение судом`, но не запрещали смежные форматные ошибки в markdown и визуально похожую латиницу в русских правовых аббревиатурах. Это закрыто валидатором и уточнением правил в `memory/topic-ssg-prototype-ui.md` / `memory/topic-visual-regression-testing.md`.

## Исправлен англоязычный `remedy` на странице дела

- На `/zpp/dela/YFjMxNYLsLlX/` в блоке `Что требовал потребитель` отображался сырой `claims_and_result.remedy`: `refund, penalty, loan interest, moral damages, removal of remaining item`.
- Причина: smoke-тест проверял только representative pages и ограниченный список видимых служебных токенов; `claims_and_result.remedy` не валидировался как пользовательский русский текст.
- Исправлено: `structure_YFjMxNYLsLlX.json` получил русскоязычный `remedy`; генератор `scripts/generate_ssg_prototype.py` добавил fallback на русские `claim_type_codes`, если `remedy` выглядит как служебный англоязычный список.
- Профилактика: `validate_structures.py` теперь падает на англоязычных служебных значениях в `claims_and_result.remedy` индексируемых JSON; `tests/smoke/site.spec.js` включает `/zpp/dela/YFjMxNYLsLlX/` в representative pages и расширенный список запрещённых видимых токенов.

## Компактный блок денежных требований без таймлайна

- Для страниц дела без таймлайна блок `Что требовал потребитель` теперь выводит `Состав требований` и `Что присуждено` в две внутренние колонки на desktop; после визуальной проверки колонки оформлены как отдельные вложенные карточки с рамкой, чтобы они не сливались внутри одной группы.
- Страницы с таймлайном сохраняют прежнюю внешнюю сетку `таймлайн + требования`.
- CSS подключается с query-версией по хэшу `prototype.css`, чтобы локальный браузер не показывал устаревшие стили после пересборки.
- Добавлен smoke-тест `case amount lists stay compact when timeline is absent`.
- Проверки: `npm run test:data` — OK, `npm run test:smoke` — 11/11, `npm run test:visual` — 14/14.

## Уточнён hero-текст ЗоПП-раздела перед деплоем

- На `/zpp/` убрана формулировка `Первые страницы судебной практики по ЗоЗПП` и число актов в lead-тексте.
- Новый акцент: сервис систематизирует судебные акты по жизненным ситуациям, требованиям, рассуждению суда, применённым нормам и повторяющимся выводам.
- Добавлена осторожная оговорка: показатели и рекомендации основаны на текущей выборке и не являются статистикой всей судебной практики.
- Обновлены smoke-ожидания и visual baseline для `/zpp/`; проверки: `npm run test:smoke` — 11/11, `npm run test:visual` — 14/14.

## Handoff перед перерывом

- Актуальная очередь: сначала закоммитить текущий пакет `practice_*.md`/валидации/SSG/UI-полиша, затем push в GitHub и проверка GitHub Actions.
- Git ожидаемо грязный: в пакете есть переобработка `practice_*.md`, усиление валидаторов/тестов, пересборка `site_prototype/`, UI-полиш `/zpp/` и обновления памяти.
- Последние проверки текущего UI-пакета: `npm run test:smoke` — 11/11, `npm run test:visual` — 14/14. Ранее в этом же пакете `npm run test:data` также проходил.

## Исправлен nested legacy-рендер норм

- На `/zpp/dela/qx9K417NRkWN/` раздел `Подробный правовой разбор` выводил вложенные пункты `Значение`/`Применение` как обычный список без левой вертикальной линии.
- Причина: `practice_qx9K417NRkWN.md` использует формат `* **Норма**` + вложенные bullets `**Значение:** ...` / `**Применение:** ...`; генератор не распознавал короткие метки как semantic note.
- Исправлено в `scripts/generate_ssg_prototype.py`: короткие метки нормализуются в `Значение в деле` / `Применение судом` и выводятся как `.md-note`.
- Smoke-тест `legacy norm bullets render as legal notes with left rail` расширен страницей `/zpp/dela/qx9K417NRkWN/` и проверкой отсутствия плоских `li strong`-меток `Значение:`/`Применение:`.
- Проверки: `npm run test:smoke` — 11/11, `npm run test:visual` — 14/14.

## Дискуссия о пределах наполнения (multi-agent)

- Открыто обсуждение `memory/discussions/content-scale-limits.md`: количественные и качественные пределы наполнения сайта с учётом алгоритмов поисковиков («Корней», SCA, E-E-A-T).
- Рамка: целевой трафик владельца **10–30K уник./сутки** (минимум 10K, желательно 30K после наполнения и масштабирования по тематикам) + постоянное дополнение новой практикой. Это превышает верхнюю KPI-веху в памяти (~3000/день) → вехи подлежат перепроверке.
- Стартовая точка: 257 активных страниц дел + 11 страниц-ситуаций на 297 актов (рост от этой базы, не с нуля).
- Состав: участники Zcode-GLM-5.2, Antigravity-Gemini-3.5-Flash, Zcode-Deepseek-v4-pro; судья Codex-5.5-xhigh.
- Раунд 1 дан Zcode-GLM-5.2: тезис — пределы = система гейтов (качество+темп+мониторинг), а не одна цифра; потолок = поведенческий/индексационный отклик; темп ступенчатый (≤100–300 → ≤300–600 → ≤600–1000/мес); «паузы уменьшить» = малые партии 50–100 + недельный цикл; срезные страницы поэтапно (фильтры → лонгриды → поштучные при плотности), матричная генерация запрещена.
- Дальше: Раунд 1 от Antigravity-Gemini-3.5-Flash и Zcode-Deepseek-v4-pro, затем Раунд 2, вердикт Codex-5.5-xhigh.
