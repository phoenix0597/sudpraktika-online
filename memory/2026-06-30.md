# 2026-06-30

## Техдолг и локальные DEV-контуры

- Актуальный технический долг вынесен в `memory/topic-technical-debt.md`.
- TD-001: 64 legacy `practice_*.md` в ЗоПП дают 78 предупреждений по practice-формату; это не ломает текущий рендер, но блокирует полный `--strict-practice-format` для всего корпуса.
- TD-002: 22 ЗоПП docid имеют legacy encoding/mojibake-артефакты; strict-кодировка обязательна для новых/изменённых docid, но полный `--strict-encoding` по всему ЗоПП-корпусу включать в CI рано.
- Локальный Docker Compose контур разделён на два сайта:
  - `http://localhost:8080/` — ЗоПП / текущий `site_prototype/`;
  - `http://localhost:8081/` — ДДУ / `dist/ddu-online.ru/`.
- Добавлен минимальный DDU-SSG: `scripts/generate_ddu_site.py`, npm-команда `npm run site:generate:ddu`.
- Сгенерировано: главная `ddu-online.ru`, 7 страниц-ситуаций, 17 страниц дел, `sitemap.xml`, `robots.txt`.

## DDU SSG: зрелый шаблон страницы дела

- DDU-страницы дел переведены с минимального шаблона на структуру, близкую к согласованному ЗоПП-шаблону: hero + disclosure, верхние панели `Итог дела`/`Что важно вынести`, блок таймлайна и требований, пользовательская история, логика решения, блок `Подробный правовой разбор`.
- Служебный блок `Стандартная структура истории` больше не выводится в DDU HTML; длинные H1 из user story ограничиваются компактным заголовком дела.
- Нормы в DDU выводятся из `legal_analysis.legal_refs` в формате с вложенными `Значение в деле`/`Применение судом` и левой вертикальной линией (`md-note`), как в зрелом ЗоПП-рендере.
- Исправлен UI-дефект вложенных узких колонок в блоке `Что требовал дольщик`: если блок требований стоит рядом с таймлайном, `Состав требований` и `Что присуждено` выводятся вертикально, чтобы длинные подписи не ломали суммы.
- Проверки после пересборки: DDU `validate_structures.py` = 68/68, `verify_all.py` = 68/68; массовый HTML-check по 17 страницам дел — обязательные блоки на месте, служебных story-маркеров нет.
