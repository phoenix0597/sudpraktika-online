# 2026-06-30

## Техдолг и локальные DEV-контуры

- Актуальный технический долг вынесен в `memory/topic-technical-debt.md`.
- TD-001: 64 legacy `practice_*.md` в ЗоПП дают 78 предупреждений по practice-формату; это не ломает текущий рендер, но блокирует полный `--strict-practice-format` для всего корпуса.
- TD-002: 22 ЗоПП docid имеют legacy encoding/mojibake-артефакты; strict-кодировка обязательна для новых/изменённых docid, но полный `--strict-encoding` по всему ЗоПП-корпусу включать в CI рано.
- Локальный Docker Compose контур разделён на два сайта:
  - `http://localhost:8080/` — ЗоПП / текущий `site_prototype/`;
  - `http://localhost:8081/` — ДДУ / `dist/ddu-online.ru/`.
- Добавлен минимальный DDU-SSG: `scripts/generate_ddu_site.py`, npm-команда `npm run site:generate:ddu`.
- Сгенерировано: главная `ddu-online.ru`, 7 страниц-ситуаций, 17 страниц дел, `sitemap.xml`, `robots.txt`.
