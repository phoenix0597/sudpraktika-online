# 2026-06-28

## Расширение словаря ситуаций ЗоЗПП после batch-3

- По подтверждению пользователя четыре спорные batch-3 ситуации переведены из кандидатных в принятые `dispute_type_code`:
  - `proper_quality_goods_exchange_art25` — ст. 25 ЗоЗПП, обмен/возврат товара надлежащего качества: 2 дела.
  - `unfair_terms_imposed_services_art16` — ст. 16 ЗоЗПП, навязанные услуги/недопустимые условия: 11 дел.
  - `harm_from_defect_art14` — ст. 14 ЗоЗПП, вред от недостатка товара/работы/услуги: 9 дел.
  - `consumer_material_damage_art35` — ст. 35 ЗоЗПП, утрата/повреждение вещи потребителя при работах: 23 дела.
- Обновлён `data/reference/zpp_enum_dictionary.json`; 45 batch-3 hold-кандидатов переведены в соответствующие новые ситуации и отмечены как индексируемые.
- Обновлены `scripts/cluster_cases.py` и `scripts/generate_ssg_prototype.py`: новые ситуации участвуют в кластеризации, навигации и генерации страниц.
- Пересобраны реестр, кластерная карта, отчёт кластеризации и `site_prototype/`: итог — 297 structured JSON, 257 активных страниц дел, 11 страниц-ситуаций.
- Проверки после изменения: `validate_structures.py` — 297/297 OK; `verify_all.py` — 297/297 OK; кастомная проверка `index_policy`/`main_site_fit` — 0 конфликтов.
- Обновлены `memory/topic-content-fill-plan.md`, `memory/topic-parsing-strategy.md`, `data/review/_TASK_PARSE_NEXT_BATCH_AGY.md` и `memory/current-work-queue.md`: эти 4 кода теперь считаются обычными валидными ситуациями, а правило про кандидатные новые ситуации остаётся только для будущих неизвестных кластеров.
- После временной проблемы кодировки при PowerShell-pipe добавлена защита в `scripts/validate_structures.py`: индексируемые JSON с маркером битой кириллицы `????` теперь не проходят валидацию. Старые hold-структуры с таким маркером не участвуют в индексе и не попадают в SSG.

## Локальный Docker/Caddy-сервер

- Добавлен `compose.yml`: `site` (`nginx:1.27-alpine`) отдаёт `site_prototype/`, `caddy` (`caddy:2-alpine`) принимает внешний порт и проксирует на `site`.
- Добавлены `.env.example` и `docker/caddy/Caddyfile`.
- Локальный адрес: `http://localhost:8080/`; контейнеры подняты через `docker compose up -d`.
- Проверено: главная и `/praktika/nekachestvennyy-tovar/` возвращают HTTP 200; UTF-8 заголовки читаются корректно.
- Для production Caddy может получить SSL при `SITE_ADDRESS=<домен>`, корректном DNS и открытых портах 80/443.

## SEO-минимум SSG

- Решение: внутренние ссылки пока не переводить с относительных на root-relative, потому что сайт корректно работает через локальный сервер; вопрос оставить как отдельную pre-production развилку.
- Реализованы canonical URL, `sitemap.xml` и `robots.txt`, формируемые из публичного `SITE_PUBLIC_URL`.
- `sitemap.xml` содержит 269 URL: главная, 11 страниц-ситуаций и 257 страниц дел.
- Проверено: sample canonical на главной/ситуации/деле корректны; `/sitemap.xml`, `/robots.txt` и страница ситуации отдаются локальным Docker-сервером с HTTP 200; `validate_structures.py` и `verify_all.py` проходят на 297/297.
- Требование закреплено в `technical-specification-mvp.md`; текущая очередь обновлена.

## UI страницы дела

- По подтверждению пользователя блок страницы дела, выводящий `case_summary.unusual_points` или `legal_analysis.arguments_rejected`, переименован с `Ошибки и риски` на нейтральное `Что важно учитывать`; причина — поле может содержать не только ошибки сторон, но и особенности акта/данных.
- `memory/topic-ssg-prototype-ui.md` очищен от устаревшего handoff-контекста 2026-06-26 и превращён в актуальный topic-файл правил UI/рендера SSG-прототипа.

## Контракт проектной памяти

- В `AGENTS.md` добавлено правило: если в сессии создавались или существенно обновлялись `memory/topic-*.md`, перед завершением шага проверять именно эти topic-файлы на временные handoff-формулировки, устаревшие статусы/счётчики и следующие шаги; устойчивые выводы оставлять в topic-файле, временное держать в daily/current queue.
- То же правило синхронизируется в шаблон проектного `AGENTS.md` репозитория `~/.codex/codex-memory-kit`.

## Smoke/visual regression tests

- Внедрён минимальный Playwright-контур для локального Docker-сервера: `package.json`, `package-lock.json`, `playwright.config.js`, `tests/smoke/site.spec.js`, `tests/visual/pages.spec.js`, эталонные снимки в `tests/__screenshots__/`.
- Smoke-тесты проверяют sitemap/robots, доступность страниц из sitemap, canonical/H1, отсутствие служебных enum-кодов в видимом UI, обязательные блоки страниц ситуации/дела и внутренние ссылки на выборке.
- Visual-тесты покрывают 6 репрезентативных страниц в desktop/mobile viewport: 12 snapshots.
- Команды: `npm run site:test`, `npm run test:smoke`, `npm run test:visual`, `npm run test:visual:update`.
- Проверено: `npm run test:smoke` — 7/7; `npm run test:visual` — 12/12; `npm run site:test` — 19/19.
- `memory/topic-visual-regression-testing.md` обновлён с фактической реализацией; текущая очередь переведена на следующий шаг — обсуждение CI/CD.

## Публичный ребрендинг под sudpraktika-online.ru

- Принято рабочее имя сервиса: **Судпрактика Онлайн** / `sudpraktika-online`.
- Причина: проект планируется шире ЗоПП; ЗоПП остаётся первым разделом, но не названием всего сервиса.
- Обновлены README, npm metadata, заголовки `technical-specification-mvp.md` и `assistant-memory.md`, а также единый бренд SSG-шапки в `scripts/generate_ssg_prototype.py`.
- Физическая локальная папка `zpp-consult` не переименовывалась: это не нужно для GitHub/CI и снижает риск поломки локального окружения.
- После пересборки прототипа и обновления visual snapshots `npm run site:test` проходит: 19/19.

## Нормализация подзаголовков user_story

- Найдена причина разного UI в блоке `История дела простым языком`: разные агенты писали подзаголовки либо как Markdown-заголовки `###`, либо как простые строки с двоеточием.
- Исправление сделано в `scripts/generate_ssg_prototype.py`: для контекста `story` известные подзаголовки истории нормализуются в `<h4>` независимо от исходного Markdown-формата.
- Добавлен smoke-тест на страницы `/dela/yz0M6YZip7Qn/` и `/dela/GMjXio3dnCrR/`, чтобы такой разнобой не вернулся.
- Проверено: `npm run test:smoke` — 8/8; `npm run site:test` — 20/20.

## Исправлен рассинхрон правового разбора по делу etduQEx0I5Wv

- На странице `/dela/etduQEx0I5Wv/` обнаружен чужой `practice_*.md`: правовой разбор был про автомобиль, пожар и ООО «Вариатор-Юг», тогда как дело относится к повреждению шин при шиномонтаже в ООО «Торговый Дом «АвтоОпт»`.
- Вероятная причина: при агентной обработке/довершении партии markdown-артефакт правового разбора был записан под неверным `docid`; прежние валидаторы проверяли наличие файлов, JSON и правовые ссылки, но не смысловое соответствие markdown-файлов делу.
- Исправлены `data/structured/user_story_etduQEx0I5Wv.md` и `data/structured/practice_etduQEx0I5Wv.md`; SSG пересобран.
- В `scripts/validate_structures.py` добавлена проверка `--strict-md-consistency`, а в `package.json` — `npm run test:data` и `npm run test:all`.
- Дополнительно добавлена диагностическая проверка `--check-user-story-format`: сейчас она показывает 145 legacy-предупреждений из 297, поэтому строгий режим подзаголовков историй не включён в общий тест до отдельной нормализации корпуса.
- Проверено: `npm run test:data` — 297/297, `verify_all.py` — 297/297, `npm run site:test` — 20/20.

## Пробная нормализация legacy user_story

- По просьбе пользователя исправлена пробная партия 15 `user_story_*.md` из 145 legacy-предупреждений: истории приведены к стандартным секциям `Кто участвовал`, `Обстоятельства и развитие событий`, `Результат для потребителя`, `Итог суда`, `Что сделано не так и как поступить правильно`.
- Для частично нормальных историй менялись только неканонические заголовки; для старых сплошных историй существующий текст разложен по секциям без внешних API.
- Строгая проверка целевых 15 файлов проходит; общий диагностический остаток `validate_structures.py --check-user-story-format` снизился со 145 до 130 предупреждений.
- SSG пересобран; проверки `npm run test:data` и `npm run site:test` проходят.

## Дополнительная нормализация legacy user_story перед перерывом

- По лимитному плану пользователя дополнительно нормализованы ещё 60 `user_story_*.md` без внешних API.
- Итого за шаг исправлено 75/145 legacy-историй; диагностический остаток `validate_structures.py --check-user-story-format` — 70 предупреждений из 297.
- Все 60 дополнительных файлов проходят `--strict-user-story-format` и `--strict-md-consistency`; SSG пересобран.
- Проверки после партии: `npm run test:data` — OK; `npm run site:test` — 20/20.

## Закрыт техдолг по стандартным подзаголовкам user_story

- Нормализованы оставшиеся 70 legacy `user_story_*.md`; внешние API не использовались.
- Итог по корпусу: 297/297 `user_story_*.md` имеют стандартные секции `Кто участвовал`, `Обстоятельства и развитие событий`, `Результат для потребителя`, `Итог суда`, `Что сделано не так и как поступить правильно`.
- Для hold-заглушек с битой кириллицей сформированы нейтральные служебные секции без публикационных выводов.
- `npm run test:data` усилен флагом `--strict-user-story-format`, чтобы формат не регрессировал в следующих партиях.
- SSG пересобран; проверки `npm run test:data` и `npm run site:test` проходят.

## Pre-production URL-решение для ЗоПП-раздела

- Принято решение до первой production-публикации и индексации перенести публичные URL ЗоПП-страниц под `/zpp/`.
- Целевые маршруты: `/zpp/`, `/zpp/praktika/<situation>/`, `/zpp/dela/<docid>/`.
- Причина: сервис «Судпрактика Онлайн» шире ЗоПП; ЗоПП — первый раздел, а не вся архитектура сайта. Лучше сделать правильную иерархию до индексации, чем позже переносить страницы с редиректами.
- Решение закреплено в `technical-specification-mvp.md`, `assistant-memory.md`, `memory/topic-ssg-prototype-ui.md` и `memory/current-work-queue.md`; генератор пока не менялся.

## GitHub-репозиторий

- Пользователь сообщил, что удалённый GitHub-репозиторий уже создан и привязан к локальному.
- Проверено: `origin = git@github.com:phoenix0597/sudpraktika-online.git`.
- Пользователь выполнил push; после `git fetch origin` локальная `main` совпадает с `origin/main` (`8bb73cb`).

## GitHub Actions CI

- Добавлен workflow `.github/workflows/ci.yml` для `push`/`pull_request` в `main`.
- В `package.json` добавлены команды `site:generate`, `test:citations` и `test:ci`.
- `npm run test:ci` выполняет: строгую валидацию structured/user_story/markdown consistency, `verify_all.py`, пересборку SSG, проверку отсутствия незакоммиченных изменений `site_prototype/`, smoke/visual Playwright-тесты.
- Workflow сохраняет Playwright HTML-report и test-results как GitHub Actions artifacts.
- Локальная проверка `npm run test:ci` прошла: data/citations/SSG/UI без ошибок.
- Обновлены README, `memory/topic-visual-regression-testing.md` и `memory/current-work-queue.md`.

## Перенос ЗоПП-раздела под `/zpp/`

- Pre-production URL-решение реализовано в SSG-прототипе: корень `/` стал общей входной страницей сервиса, ЗоПП-каталог перенесён в `/zpp/`.
- Новые маршруты: `/zpp/`, `/zpp/praktika/<situation>/`, `/zpp/dela/<docid>/`.
- Старые root-level каталоги `site_prototype/praktika/` и `site_prototype/dela/` больше не генерируются.
- `sitemap.xml` содержит 270 URL: общая главная, `/zpp/`, 11 страниц-ситуаций и 257 страниц дел.
- Обновлены generator, canonical URL, внутренние ссылки, smoke/visual-тесты и visual snapshots.
- Проверки: `npm run test:data` — OK; `npm run test:citations` — 297/297, 0 галлюцинаций; `npm run site:test` — 23/23.
- Обновлены `technical-specification-mvp.md`, `assistant-memory.md`, README, `memory/topic-ssg-prototype-ui.md`, `memory/topic-visual-regression-testing.md` и `memory/current-work-queue.md`.

## Исправлен рендер legacy-норм в правовом разборе

- На странице `/zpp/dela/BRRlQN72V9V6/` пользователь заметил, что в блоке `Подробный правовой разбор` раздел `Нормы, на которые сослался суд` отрендерился обычным списком без `.md-note` и левой вертикальной линии.
- Первопричина: генератор нормализовал legacy-строки вида `**Норма**: текст`, но не обрабатывал распространённый формат `**Норма** — текст`.
- Исправлено в `scripts/generate_ssg_prototype.py`: тире и двоеточие считаются допустимыми разделителями legacy-норм.
- Добавлен smoke-тест на `/zpp/dela/BRRlQN72V9V6/`, который проверяет наличие `.md-note` и CSS-левой линии.
- Проверено по всем сгенерированным страницам дел: страниц с разделом норм без `.md-note`/`legal-ref` — 0.
- Проверки: `npm run test:data` — OK; `npm run test:smoke` — 10/10.

## Техдолг по неполному применению норм в legacy practice

- После исправления рендера выявлено, что часть legacy `practice_*.md` содержит пояснение нормы, но не содержит отдельного подпункта `Применение судом`.
- Временная идея автозаглушки `Суд сослался на эту норму при разрешении спора` отклонена: она малополезна и маскирует недоразметку.
- Точный долг по опубликованным страницам: 13 docid / 222 пункта норм. Список закреплён в `data/review/practice-norm-application-debt.txt`.
- Добавлена проверка `scripts/check_practice_norm_application_debt.py`, подключена в `npm run test:data`: новые такие случаи будут падать тестом, а исправление известных потребует обновить debt-list.
- В очередь добавлена задача переобработать эти 13 страниц перед PROD.

## Следующий шаг

После коммита — отправить локальные изменения в GitHub и проверить первый GitHub Actions run; затем подготовить PROD-deploy контур.
